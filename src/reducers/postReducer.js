import { FETCH_POSTS } from '@/actions/types'

const initialState = {
    items: [{name: 'Rafael'}]
}

export default (state = initialState, action) => {
    switch(action.type){
        default: 
            return state
    }
}